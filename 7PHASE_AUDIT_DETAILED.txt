╔═══════════════════════════════════════════════════════════════════════════════╗
║                 7-PHASE SYSTEM REMNANTS - DETAILED AUDIT                      ║
║                        LINE-BY-LINE FINDINGS                                  ║
╚═══════════════════════════════════════════════════════════════════════════════╝

SEARCH RESULTS SUMMARY
─────────────────────────────────────────────────────────────────────────────────
Total Files with 7-Phase References: 6 active code files (+ doc files)
Total Issues Found: 3 CRITICAL + 2 INFORMATIONAL

═══════════════════════════════════════════════════════════════════════════════
[CRITICAL ISSUE #1] PHASE VALIDATION ALLOWS 1-7
═══════════════════════════════════════════════════════════════════════════════

FILE: tools/update-trip-info.js
LINES: 123-124

63    async function execute(args) {
64      const { constraints, phase, itinerary } = args || {};
65
66      // 基本验证
67      if (!constraints && phase === undefined && !itinerary) {
68        return JSON.stringify({ error: '至少需要传入 constraints、phase、itinerary 中的一个' });
69      }
70
71      // 构建回执（server.js 会从 tool result 中读取并写入 TripBook）
72      const updates = {};
73      const messages = [];
74
75      // 字段名中→英翻译映射（供用户可见的标签）
76      const FIELD_LABELS = {
77        destination: '目的地',
78        departCity: '出发城市',
79        dates: '日期',
80        people: '人数',
81        budget: '预算',
82        preferences: '偏好',
83        specialRequests: '特殊需求',
84      };
85
86      // 内部7阶段 → 面板4阶段映射（与前端 itinerary.js 的 mapPhase 保持一致）
87      function mapPhaseToDisplay(raw) {
88        if (raw <= 1) return { num: 1, label: '确认需求' };
89        if (raw <= 3) return { num: 2, label: '规划行程' };
90        if (raw <= 5) return { num: 3, label: '完善细节' };
91        return { num: 4, label: '预算总结' };
92      }
93
94      if (constraints) {
...
121    }
122
>>> 123    if (phase !== undefined) {
>>> 124      if (phase < 1 || phase > 7) {  ← ⚠️ ALLOWS UP TO PHASE 7
>>> 125        return JSON.stringify({ error: 'phase 必须在 1-7 之间' });  ← ⚠️ ERROR MESSAGE REFERENCES 1-7
126      }
127      updates.phase = phase;
128      const display = mapPhaseToDisplay(phase);
129      messages.push(`${display.label}（${display.num}/4）`);
130    }

ISSUE SEVERITY: 🔴 CRITICAL
────────────────────────────
• Line 124: Validation allows phase > 4
• Line 125: Error message explicitly mentions "1-7" range
• The mapPhaseToDisplay() function (lines 87-92) is intentionally handling phases > 4
  BUT the system should NOT accept them if only 4 phases are supposed to exist
• This enables AI to call update_trip_info with phase=5, 6, or 7

FIX:
─────
Change line 124 to: if (phase < 1 || phase > 4) {
Change line 125 to: return JSON.stringify({ error: 'phase 必须在 1-4 之间' });

═══════════════════════════════════════════════════════════════════════════════
[CRITICAL ISSUE #2] TOOL DEFINITION DESCRIBES NON-EXISTENT PHASES
═══════════════════════════════════════════════════════════════════════════════

FILE: tools/update-trip-info.js
LINES: 15-61 (TOOL_DEF object)

15    const TOOL_DEF = {
16      name: 'update_trip_info',
17      description: [
18        '更新行程参考书。当确认用户需求、完成信息查询、或推进行程规划时调用此工具。',
19        '调用时机：1）用户明确了目的地/日期/人数/预算等约束信息后；',
20        '2）查询到机票/酒店/天气等信息需要记录时；',
21        '3）推进到新的规划阶段或生成行程框架时。',
22        '传入的数据为增量更新，只需传变化的字段。'
23      ].join(''),
24      parameters: {
25        type: 'object',
26        properties: {
27          constraints: { ... },
28          phase: {
29            type: 'integer',
>>> 30            description: '当前规划阶段（1-7）：1锁定约束 2机票查询 3构建框架 4关键预订 5每日详情 6预算汇总 7导出总结'
                             ^^^^^^^ LISTS ALL 7 PHASES
31          },
32          itinerary: { ... }
33        }
34      }
35    };

PHASE DEFINITIONS IN TOOL DESCRIPTION:
────────────────────────────────────────
1. 锁定约束 (Lock constraints) ✓ EXISTS → Phase 1 on display
2. 机票查询 (Flight search) ✓ EXISTS → Phase 2 on display
3. 构建框架 (Build framework) ✓ EXISTS → Phase 2 on display (mapping)
4. 关键预订 (Key bookings) ? AMBIGUOUS → Maps to Phase 3
5. 每日详情 (Daily details) ✗ REMOVED → Was Phase 3, now part of Phase 3
6. 预算汇总 (Budget summary) ✗ REMOVED → Now Phase 4
7. 导出总结 (Export summary) ✗ REMOVED → Never used

ISSUE SEVERITY: 🔴 CRITICAL
────────────────────────────
• Tool description is sent to AI as part of its system prompt
• AI reads "phases 1-7" and may assume it can set phases 5, 6, 7
• The phase descriptions don't match current 4-phase system:
  - "关键预订" (Key bookings) at phase 4 vs current "预算总结" (Budget summary)
  - "每日详情" (Daily details) was its own phase, now merged
  - "导出总结" (Export summary) no longer exists

FIX:
─────
Update line 30 to match current system (OPTION 1 - Simple):
description: '当前规划阶段（1-4）：1锁定约束 2机票查询 3构建框架 4预算总结'

OR (OPTION 2 - With mapping explanation):
description: '当前规划阶段（1-4）：1锁定约束 2机票查询 3构建框架 4预算总结。注：内部可使用1-7作为中间状态，自动映射到1-4显示'

═══════════════════════════════════════════════════════════════════════════════
[CRITICAL ISSUE #3] PHASE 7 TRIGGERING LOGIC IN SERVER
═══════════════════════════════════════════════════════════════════════════════

FILE: server.js
LINES: 740-776

740     // ── 自动推进 phase ──
741     try {
742       const currentPhase = (tripBook.itinerary && tripBook.itinerary.phase) || 0;
743       const toolNames = toolCalls.map(tc => tc.name);
744       let inferredPhase = currentPhase;
745
746       if (currentPhase < 2 && toolNames.some(n => ['delegate_to_agents', 'search_flights'].includes(n))) {
747         inferredPhase = 2;
748       } else if (currentPhase < 3 && toolNames.some(n => ['search_hotels', 'search_poi'].includes(n))) {
749         inferredPhase = 3;
750       }
751
752       // phase 4 自动推进：当 update_trip_info 写入了 budgetSummary 时
753       if (currentPhase < 4 && toolNames.includes('update_trip_info')) {
754         for (const tc of toolCalls) {
755           if (tc.name === 'update_trip_info') {
756             try {
757               const args = tc.args || {};
>>> 758               if (args.itinerary?.budgetSummary || args.phase === 4 || args.phase === 7) {
                                                                            ^^^^^^^^^^^^^^^^ ⚠️ DEAD CODE
759                 inferredPhase = 4;
760               }
761             } catch {}
762           }
763         }
764       }

ISSUE SEVERITY: 🔴 CRITICAL (Dead Code)
─────────────────────────────────────────
• Line 758: Checks for "args.phase === 7" but phase 7 should never exist
• This is vestigial code from the old 7-phase system
• Comment on line 752 suggests phases were once tracked differently
• The logic is: "if budget summary exists OR phase is 4 OR phase is 7, set display to 4"
• Phase 7 trigger is unreachable if validation in tools/update-trip-info.js works correctly

CONTEXT: Phase Mapping Logic in server.js (lines 307-312)
──────────────────────────────────────────────────────────
307             // AI 内部 phase 1-7 → 面板 phase 1-4 映射
308             let displayPhase = updates.phase;
309             if (displayPhase <= 1) displayPhase = 1;
310             else if (displayPhase <= 3) displayPhase = 2;
311             else if (displayPhase <= 5) displayPhase = 3;
312             else displayPhase = 4;

This mapping shows the original intent:
  - Internal phase 1 → Display 1
  - Internal phase 2-3 → Display 2
  - Internal phase 4-5 → Display 3
  - Internal phase 6-7 → Display 4

BUT if we're only using 1-4 internally, this mapping becomes unnecessary.

FIX:
─────
Remove the "|| args.phase === 7" condition on line 758:
FROM: if (args.itinerary?.budgetSummary || args.phase === 4 || args.phase === 7) {
TO:   if (args.itinerary?.budgetSummary || args.phase === 4) {

═══════════════════════════════════════════════════════════════════════════════
[INFORMATIONAL] PHASE MAPPING LOGIC (WORKING CORRECTLY)
═══════════════════════════════════════════════════════════════════════════════

FILE: server.js
LINES: 300-322

300         // update_trip_info → 核心：写入 TripBook 约束/行程/阶段
301         if (funcName === 'update_trip_info' && parsed.success && parsed.updates) {
302           const updates = parsed.updates;
303           if (updates.constraints) {
304             tripBook.updateConstraints(updates.constraints);
305           }
306           if (updates.phase !== undefined) {
307             // AI 内部 phase 1-7 → 面板 phase 1-4 映射
308             let displayPhase = updates.phase;
309             if (displayPhase <= 1) displayPhase = 1;
310             else if (displayPhase <= 3) displayPhase = 2;
311             else if (displayPhase <= 5) displayPhase = 3;
312             else displayPhase = 4;
313             tripBook.updatePhase(displayPhase);
314           }

STATUS: ✓ WORKING CORRECTLY
─────────────────────────────
This mapping is:
  • Intentional design choice
  • Currently working as designed
  • Documented in comments
  • Part of a historical architecture decision

RECOMMENDATION:
Since we're no longer using phases 5-7, consider either:
  a) Remove the mapping and use 1-4 directly
  b) Keep as-is with updated comments explaining it's legacy code
  c) Document that this enables future expansion to more phases

═══════════════════════════════════════════════════════════════════════════════
[INFORMATIONAL] PHASE LABELS IN TRIP-BOOK MODEL
═══════════════════════════════════════════════════════════════════════════════

FILE: models/trip-book.js
LINES: 10-16

10    const PHASE_LABELS = [
11      '',            // 0 — 未开始
12      '了解需求',    // 1 — 了解用户需求 + 锁定硬性约束
13      '规划框架',    // 2 — 大交通 + 行程框架
14      '完善详情',    // 3 — 填充景点/餐饮/住宿
15      '行程总结',    // 4 — 预算汇总
16    ];

STATUS: ✓ CORRECT
────────────────
• PHASE_LABELS array has exactly 5 entries (indices 0-4)
• Index 0 = unstarted, indices 1-4 = four phases
• Correctly maps to 4-phase display system
• Used by TripBook.updatePhase() at line 136-140:

136   updatePhase(phase) {
137     if (typeof phase === 'number' && phase >= 0 && phase <= 4) {
138       this.itinerary.phase = phase;
139       this.itinerary.phaseLabel = PHASE_LABELS[phase] || '';
140     }
141   }

This validation is CORRECT and enforces phase <= 4.

═══════════════════════════════════════════════════════════════════════════════
[INFORMATIONAL] DOCUMENTATION REFERENCES
═══════════════════════════════════════════════════════════════════════════════

Files containing "7阶段" or "1-7" references (documentation only):

1. PRACTICAL_INFO_ANALYSIS.md : Line 136
   phase: integer,        // 当前规划阶段（1-7）
   
2. docs/ARCHITECTURE.md : Lines 134, 298
   ├── methodology.js # 7阶段规划方法论
   │ 5. 渐进式方法论 (7阶段)
   
3. docs/PRD.md : Line 104
   - 规划阶段进度（Phase 1-7）
   
4. prompts/system-prompt.js : Line 85
   - **绝对不要**因为用户修改了第3天就把第1-7天全部重传
   (NOTE: This is referring to days 1-7, NOT phases, so it's correct)

RECOMMENDATION:
These are documentation files. Update them for consistency, but they're lower
priority than the code changes.

═══════════════════════════════════════════════════════════════════════════════
PHASE SYSTEM COMPARISON TABLE
═══════════════════════════════════════════════════════════════════════════════

                      OLD (7-Phase)        CURRENT (4-Phase)    MAPPING
─────────────────────────────────────────────────────────────────────────────
Phase 1               锁定约束             了解需求             1 → 1 ✓
Phase 2               机票查询             规划框架             2 → 2 ✓
Phase 3               构建框架             完善详情             3 → 2 (merged)
Phase 4               关键预订             预算总结             4 → 3 (merged)
Phase 5               每日详情             (merged to 3)        5 → 3 (merged)
Phase 6               预算汇总             (merged to 4)        6 → 4 (merged)
Phase 7               导出总结             (removed)            7 → 4 (removed)

═══════════════════════════════════════════════════════════════════════════════
SEARCH QUERIES USED
═══════════════════════════════════════════════════════════════════════════════

To find these remnants, the following grep patterns were used:

1. grep -rn "phase.*[567]" (literal phase with 5, 6, or 7)
2. grep -rn "phase < 1 || phase > 7" (validation range)
3. grep -rn "1-7" (date/phase range references)
4. grep -rn "7阶段|七阶段" (Chinese 7-phase references)
5. grep -rn "mapPhase|mapPhaseToDisplay" (phase mapping functions)
6. grep -rn "phase >= 1 && phase <= 7" (range checks)

═══════════════════════════════════════════════════════════════════════════════
IMPACT ASSESSMENT
═══════════════════════════════════════════════════════════════════════════════

If not fixed, potential issues:

1. IMMEDIATE:
   ✗ AI could call update_trip_info with phase=5/6/7
   ✗ Frontend may not display phases > 4 correctly
   ✗ Validation inconsistency between tool and TripBook

2. MEDIUM-TERM:
   ✗ Confusion for future developers reading the code
   ✗ Harder to debug phase-related issues
   ✗ Documentation mismatch with actual behavior

3. LONG-TERM:
   ✗ Legacy code debt increases
   ✗ Harder to implement new features if phase system changes
   ✗ Risk of regression if code is refactored

═══════════════════════════════════════════════════════════════════════════════
